# 本仓的字节是产品的一部分，不能随机器 git 配置变。
#
# 没有这个文件时，本机 core.autocrlf=true 会在每次检出把工作树换成 CRLF，而
# 三个运行端的 Marketplace 直接指向工作树——于是运行端装到的是 CRLF，仓库里
# 存的是 LF，符合性测试与语料预算又都按 LF 规范化计算。量的和发的不是同一份
# 东西，而且检出时机不同还会得到不同结果。
#
# 2026-08-15 实测到这个形状：同一个 0.2.6，从新写入的工作树装出来是 LF，合并
# 后重新检出的工作树是 CRLF，plugin_release 的整树指纹据此判为 modified。
#
# 统一钉 LF：仓库、工作树、安装副本与所有字节统计从此是同一个口径。
* text=auto eol=lf
